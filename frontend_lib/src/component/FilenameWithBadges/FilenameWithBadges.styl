.FilenameWithBadges
  display flex
  align-items baseline
  &__label
    overflow hidden
    text-overflow ellipsis
    unicode-bidi plaintext
    font-weight bold
  & .badge
    direction ltr

.tracimTable__body__row__cell .badge
  overflow visible
